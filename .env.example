# EmailJS credentials for the contact form.
# These are browser-visible values (EmailJS public keys), not secrets.
# Copy this file to `.env.local` and fill in your own values to override the defaults.

REACT_APP_EMAILJS_SERVICE_ID=
REACT_APP_EMAILJS_TEMPLATE_ID=
REACT_APP_EMAILJS_PUBLIC_KEY=
